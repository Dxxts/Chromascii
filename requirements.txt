opencv-python<5  # 5.x wheels have dropped CascadeClassifier, breaking --face-censor
Pillow
numpy
rich>=13.0
colorama
yt-dlp
# optional — enables --virtual-cam (also needs OBS Virtual Camera driver on Windows)
# pyvirtualcam
# optional — enables audio playback for video (no system ffmpeg binary needed)
# av
# sounddevice
